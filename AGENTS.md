# Reader Code Rules

- 阅读器管理端代码位于 `src/api/reader/**`、`src/views/reader-admin/**`、`src/router/modules/reader-admin.ts`。
- 从 2026-08-14 起，所有阅读器需求的新增或修改代码都必须同步补齐注释。
- 页面、API、路由、类型文件都要有文件级注释。
- 接口字段、查询参数、页面核心函数、事件处理函数、异步加载流程都要补注释。
- 关键 import 分组、请求工具引入、共享类型引入、路由依赖和页面核心状态声明也要补说明注释，不能只注释函数。
- 目录预览、章节预览、上传导入、上下架、状态回显、路由跳转等关键交互必须补充流程注释。
